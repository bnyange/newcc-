#include <stdio.h>

int main(){
    
    float result; /*The result of the division */
    float a = 5.0; // change both data types into float
    float b = 13.5;

    result = a / b;
    printf("the result is %f\n", result); // change into %f
    return 0;
}